services:
  - type: web
    name: product-creation-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/start_backend.py
    envVars:
      - key: PORT
        value: 8000
      - key: CORS_ORIGINS
        value: "*"
      - key: UPLOAD_DIR
        value: "backend/uploads"
      - key: CONFIG_DIR
        value: "backend/config"
      - key: LOG_DIR
        value: "backend/logs"
      - key: PYTHONPATH
        value: "/opt/render/project/src/backend"
    region: oregon
    healthCheckPath: /docs